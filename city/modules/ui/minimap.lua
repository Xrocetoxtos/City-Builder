local M = {}

    function M.load()

    end

    function M.update()
        
    end

    function M.draw()               -- VEEL TE TRAAAG
        -- for x=2, 256,2 do
        --     for y=2, 256,2 do
        --         local x1 = SCREEN_WIDTH - (256-x)/4 -12
        --         local y1 = SCREEN_HEIGHT - (256-y)/4 - 32
        --         if Map.pathfindingMap[y][x] == 1 then
        --             Colours.setColour(Colours.BLUE)
        --         else
        --             Colours.setColour(Colours.WHITE)
        --         end
        --         love.graphics.rectangle("line", x1, y1, 1, 1)
        --     end
        -- end
    end

return M